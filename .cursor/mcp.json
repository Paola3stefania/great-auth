{
  "mcpServers": {
    "better-auth-agent": {
      "command": "npx",
      "args": ["better-auth", "agent", "--url", "http://localhost:4000", "--providers", "github"],
      "env": {
        "BETTER_AUTH_ENCRYPTION_KEY": "dev-encryption-key-change-in-prod"
      }
    }
  }
}
