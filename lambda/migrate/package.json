{
  "name": "migration-lambda",
  "version": "1.0.0",
  "description": "Lambda function to run database migrations",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r migration-lambda.zip . -x '*.ts' '*.tsx' 'node_modules/@types/**' '*.map' '*.json' 'README.md' 'deploy.sh'"
  },
  "dependencies": {
    "drizzle-orm": "^0.43.1",
    "postgres": "^3.4.5",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^22.15.18",
    "typescript": "^5.8.3"
  }
}
